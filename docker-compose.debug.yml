version: '3.1'
services:
  web:
    build:
      context: .
      dockerfile: ./docker/Dockerfile
      target: debug-odoo
    image: odoo-debug:${VERSION-17.0}
    ports:
      - "5678:5678"
    volumes:
      - ./src/odoo:/app